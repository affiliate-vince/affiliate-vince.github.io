# 2026-04-02

## Session 3: 重构 v3 — Amazon Store 联盟矩阵（2026-04-02 19:10）

**完成内容：**
- 网站第三次重建，完全脱离 Kickstarter/Indiegogo，转为 **Amazon Store 联盟 Landing Page 矩阵**
- 删除旧的 products/、static/、data/ 等目录（git 已跟踪删除）
- 新架构：单页 index.html + manifest.json 映射表 + SKILL.md 维护指南
- 抓取 18 个 Amazon Store 品牌信息（名称、分类、描述、Logo URL）
- 主页：5个随机 Banner 轮播（2秒自动翻页）+ 18个品牌卡片网格
- 联盟链接直接从 landingpages.txt 读取 UUID 并映射
- SEO：WebSite Schema.org + OG meta + robots.txt 禁止抓取 manifest/landingpages
- 创建 SKILL.md：维护流程、变更检测逻辑、禁止事项

**文件清单（v3）：**
- index.html（含 BRANDS[]、AFFILIATE_URLS{}、BRAND_UUIDS{}）
- manifest.json（品牌映射表，UUID → 品牌 → 联盟链接）
- landingpages.txt（21条 Amazon Store 联盟链接）
- sitemap.xml、robots.txt、SKILL.md

---

## Session 2: 重建 Vince Media (v2) — 2026-04-02 18:17

**完成内容：**
- 完全重建网站，品牌名更正为 **Vince Media**（原 BackTheNew）
- 删除旧的 data/products.json、generate-pages.py、update.js 等脚本
- 架构转为：每个产品一个独立 HTML 文件，全部静态，无 JS 依赖
- 所有媒体素材引用本地 `static/` 目录（不再外链 Kickstarter CDN）
- 主页卡片直连联盟链接，极短行为路径（首页 → 直接到落地页）
- 4个产品页全部重写：rewindpix.html、spectraeyes.html、ingym.html、ideephealth.html
- 内容全部真实：RewindPix规格（$109早鸟/Sony传感器）、SpectraEyes（60Hz热成像/500m）、InGym（自适应电机）、iDeepHealth（TCM+AI+ETH Zurich验证）
- iDeepHealth页面加了"非医疗设备"免责声明
- sitemap.xml、robots.txt 更新（robots.txt 禁止爬取 /static/）

**维护方式（v2）：**
- 增加产品：在 `static/` 新建文件夹 → 放入素材和 `landingpage.txt` → 新建 `products/xxx.html` → 更新 sitemap.xml → 主页加卡片

---

## Session: 构建 BackTheNew Kickstarter 联盟落地页矩阵

**完成内容：**
- 从 landpages.txt 的 21 个 Kickbooster 联盟链接构建了完整静态网站
- 网站名称：BackTheNew
- 创建了 `data/products.json`，包含 21 个产品的完整结构化数据（搜索获取）
- 构建了 `index.html` 主页，数据驱动渲染、分类筛选、搜索、排序功能
- 用 Python 生成了 21 个独立 SEO 落地页（`products/<slug>.html`）
- 创建了 `update.js`（Node.js）维护脚本：diff landpages.txt vs products.json
- 创建了 `generate-pages.py`（Python）页面生成脚本
- 创建了 `sitemap.xml`、`robots.txt`、`README.md`
- 全站 SEO：Schema.org Product/BreadcrumbList/WebSite、OG tags、Twitter Card
- 本地预览已通过 http://127.0.0.1:8765 验证

**维护流程：**
1. 修改 `landpages.txt`（增减链接）
2. 运行 `node update.js` 检测差异（或手动编辑 products.json）
3. 补全新产品的 name/description/imageUrl 等字段
4. 运行 `python generate-pages.py` 重新生成落地页
5. git commit & push
